# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: build-tilediiif.tools
on:
  pull_request: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - {}
