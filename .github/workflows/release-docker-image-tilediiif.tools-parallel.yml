# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-docker-image-tilediiif.tools-parallel
on:
  push:
    tags:
      - docker/tilediiif.tools-parallel-v*
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v2
      - run: npx projen build-docker-image:tilediiif.tools-parallel
      - run: npx projen push-docker-image:tilediiif.tools-parallel
    container:
      image: ghcr.io/cambridge-collection/tilediiif/dev-environment:v0.1.4-tools
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
